export interface IngresoEstacionValues {
  description: {
    value: string;
    metadata: object;
  };
  location: {
    coordinates: string[];
    metadata: object;
  };
  user: {
    value: string;
  };
}
